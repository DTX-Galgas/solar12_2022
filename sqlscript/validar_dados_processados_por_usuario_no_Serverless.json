{
	"name": "validar_dados_processados_por_usuario_no_Serverless",
	"properties": {
		"content": {
			"query": "SELECT * FROM sys.dm_external_data_processed\n--\nselect\nlogin_name,\n(substring(convert(varchar(10), start_time,103), 4, 10)) Data,\nsum(data_processed_mb) sum_data_processed_mb\nfrom sys.dm_exec_requests_history\ngroup by login_name, (substring(convert(varchar(10), start_time,103), 4, 10))",
			"metadata": {
				"language": "sql"
			},
			"currentConnection": {
				"databaseName": "master",
				"poolName": "Built-in"
			},
			"resultLimit": 5000
		},
		"type": "SqlQuery"
	}
}